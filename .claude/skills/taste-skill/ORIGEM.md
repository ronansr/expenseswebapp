# De onde veio esta skill

`github.com/Leonxlnx/taste-skill` (pasta `skills/taste-skill/`), commit `ccbc156`,
copiada em 24/08/2026. O repositorio traz varias skills; foi copiada so esta.

**Uma alteracao, de proposito:** o `name` do frontmatter era
`design-taste-frontend` e passou a ser `taste-skill`, para casar com o nome da pasta
— e o nome da pasta e o que se digita para invocar. Ao atualizar, reaplique essa linha.

⚠️ **Ela e de landing page, portfolio e redesign — nao de painel.** O proprio
`SKILL.md` diz isso na primeira linha: "not dashboards, not data tables, not multi-step
product UI". Neste repositorio o alcance dela e o site institucional (`(site)`), o
diretorio `/lojas` e o cardapio; o `/painel`, o `/admin` e o `/motoboy` sao
ferramenta de trabalho e seguem a regua do `CLAUDE.md`.

⚠️ **Ela nao substitui `design-motion-principles`.** Aquela decide movimento e e o
padrao obrigatorio do produto; esta decide direcao visual. Numa tela nova de vitrine,
as duas.
